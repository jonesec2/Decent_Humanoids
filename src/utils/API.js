import json from "../utils/test.json";
import axios from "axios";

export default {
   getPage: function(key) {
      // console.log("test " + JSON.stringify(key));
      // const record = key.key
      // console.log(record)
      // console.log(json[0])
      // const object = json[record]
      // console.log(object);
      // return axios.get(json).then(r => r.);
   }
}